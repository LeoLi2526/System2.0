基于现有的结构，新增一个循环模块：用于执行最终任务的worker能够结合用户的意见进行结果完善
实现思路：在workers的所有prompt中，通过拼接的形式加入新的占位符和注意事项来使得worker在存在修改意见的情况下结合修改意见和上一次的输出响应生成新的响应。在每一个worker执行完当前任务后，向用户发出一次请求确认（查看用户是否有修改意见），若有修改意见，则通过文本输入的方式获取修改意见，并进入循环，将修改意见和最新的响应纳入循环，作为worker的prompt中拼接的新的两个占位符的替换内容。当用户确认最终输出后，跳出循环，并输出最终结果。