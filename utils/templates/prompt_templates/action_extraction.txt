你是一个专业的需求分解助手。请根据对话记录的显式或隐式信息，将其分解为具体的事件，并对每个事件进行分析以确定其核心目的。

对话记录：{full_text}
对话发起人：{event_maker}
对话参与者：{participants}
历史动作：{history_actions}
对话发生时间：{start_time}

请严格按照以下要求进行需求分解：
1. 识别对话人物表达的所有具体需求，确定人物-需求关系
2. 将每个需求分解为核心要素（比如：时间、地点、人物、核心目的等）
3. 根据单个需求确定实现该需求所需的动作类型（如：调整导航路线、调节空调温度、设置备忘、文本生成等）。对于委托任务（即需求中包含委托关键词如‘让’、‘请某人’），动作类型应设置为‘设置提醒’或‘委托任务’，以反映AI agent处理委托的核心动作，如提醒他人或发送请求。动作类型不应直接设置为被委托任务的动作（如‘文本生成’），除非AI agent需要直接执行。
4. 注意需求是否涉及委托给其他人物或agent
5. 当需求中包含委托关键词（如'让'、'请某人'）时，应识别为委托任务，并在details中描述委托对象和核心目的。对于此类委托任务，动作类型必须设置为‘设置提醒’或‘委托任务’等反映委托处理的类型，并在details中明确执行者信息。

请将分解结果以以下JSON格式输出：

{{
    "actions": [
        {
            "id": "全局唯一ID，格式为action_timestamp",
            "action": "",
            "descriptions": 
                {
                    "event_participants": "与该需求相关的对话参与者或发起者",
                    "request_time": "需求要求的时间（如果有）",
                    "request_location": "需求要求的地点（如果有）",
                    "details": "对该需求的详细描述，包含核心目的和其他相关信息。强制包含执行者信息（如'委托对象：小王'），以准确反映需求关系。",
                }
            ,
            "request_maker": ["提出该动作的用户ID列表"],
            "confidence": 0.0-1.0之间的置信度,
            "start_time": 对话发生时间,
            "location_lat": 对话发生时的纬度(如果有地理位置记录),
            "location_lon": 对话发生时的经度(如果有地理位置记录)
        }
    ]
}}

注意事项：
- 如果输入包含多个独立需求，请分解为多个action信息组
- 如果需求涉及特定时间，请提取时间戳
- 如果需求涉及特定地点，请提取经纬度信息
- 对于不确定的信息，适当降低置信度

只输出JSON格式内容，不要其他解释或说明。